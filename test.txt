Understand how the logic from course notes works

load json file on courses.

prompt from main.py is fed into process_user_message

process_user_message
    defines delimiter
    feeds user prompt into identify_category_and_course to determine the relevant courses as a dictionary
    fed in as a message not just a prompt, contextual information is placed into the system message end.
    user prompt 

    output from identify_category_and_course is fed into get_course_details to obtain the course details

    output from get_course_details and the user prompt is fed into is fed into generate_response to 
    
